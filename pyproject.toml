[project]
name = "omega"
version = "1.0.0"
description = "Agente Omega — núcleo híbrido PT-BR"
requires-python = ">=3.12"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["testes"]
pythonpath = ["."]
